<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cashRegisterAndroidApp">

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CashRegisterAndroidApp">

        <activity
            android:name="com.cashRegisterAndroidApp.cameraModule.CameraActivity"
            android:label="Camera Activity">
        </activity>

        <activity
            android:name="com.cashRegisterAndroidApp.cameraModule.CameraActivity2"
            android:label="Camera Activity2">
        </activity>

        <activity
            android:name="com.cashRegisterAndroidApp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
